# n8n (opcional)
# Si no defines estas variables, la app funciona igual (solo localStorage + mocks).

# Webhook para recibir los resultados (POST JSON)
VITE_N8N_RESULTS_WEBHOOK_URL="https://genaroroustan1.app.n8n.cloud/webhook/ampm-results"

# Endpoint para validar tokens (GET ?token=... -> { valid: boolean, expired?: boolean })
VITE_N8N_TOKEN_VALIDATE_URL="https://TU-N8N/webhook/ampm-validate-token"
